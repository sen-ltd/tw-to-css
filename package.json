{
  "name": "tw-to-css",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Paste Tailwind classes, get vanilla CSS. A minimal Tailwind-class-to-CSS converter — tokenizer, lookup tables, live preview. Vanilla JS, no build step.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8080"
  },
  "license": "MIT"
}
